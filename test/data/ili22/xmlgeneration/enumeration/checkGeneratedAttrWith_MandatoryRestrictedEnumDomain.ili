INTERLIS 2.2;

MODEL model =

  TOPIC topic =

    DOMAIN
      Farbe = (rot,
           gelb,
           gruen);
	   
    CLASS a =
      farbe : Farbe;
    END a;
    CLASS b EXTENDS a=
      farbe (EXTENDED): MANDATORY;
    END b;
  END topic;
END model.
