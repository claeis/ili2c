package ch.interlis.ili2c.generator.java;

import ch.interlis.ili2c.metamodel.*;

public class JavaGenerator
{
	/**
	* Top level method to generate JAVA sources.
	*
	 * @param datamodel is the TransferDescription with all the models, topics,
	 *     classes and further definitions that want to be converted into a JAVA
	 *     representation.
	 * @param baseDir is the name of the base directory.
	 * @param prefix is the package prefix of the generated source code.
	 * @return int returns the numbers of errors while generating.
	*/
	public static int generate(TransferDescription datamodel, String baseDir, String prefix)
	{
		return 0;
	}
}
